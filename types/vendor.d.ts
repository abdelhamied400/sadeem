/**
 * Bootstrap 5 ships no types for its prebuilt browser bundle. It is imported
 * for its side effects only (dropdowns/tabs on the legacy `dev_` pages), so an
 * empty module declaration is all that is needed.
 */
declare module "bootstrap/dist/js/bootstrap";
